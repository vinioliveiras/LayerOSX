---
# Só a página de boas-vindas + link de arranque da partição. Sem checks
# de internet (o unpackfs não precisa de rede, tudo já vem no ISO).
showSupportUrl: false
showKnownIssuesUrl: false
showReleaseNotesUrl: false
showDonateUrl: false

requirements:
    requiredStorage: 40.0
    requiredRam: 2.0
    check:
        - storage
        - ram
        - power
    required:
        - storage
        - ram
