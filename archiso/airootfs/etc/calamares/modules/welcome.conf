---
# Just the welcome page + the link into partitioning. No internet
# checks (unpackfs doesn't need network, everything already ships on
# the ISO).
showSupportUrl: false
showKnownIssuesUrl: false
showReleaseNotesUrl: false
showDonateUrl: false

requirements:
    requiredStorage: 40.0
    requiredRam: 2.0
    check:
        - storage
        - ram
        - power
    required:
        - storage
        - ram
